server:
  port: 8082
spring:
  application:
    name: vendas
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb://localhost:27018/db_vendas}
jwt:
  secret: ${JWT_SECRET:dev-secret-muito-longo-para-hmac-sha256}
clientes:
  url: ${CLIENTES_URL:http://localhost:8080}
produtos:
  url: ${PRODUTOS_URL:http://localhost:8081}
springdoc:
  swagger-ui:
    path: /swagger-ui.html
management:
  endpoints:
    web:
      exposure:
        include: health
